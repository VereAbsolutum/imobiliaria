<button type="button" class="bg-red-900 text-white px-4 py-2 rounded flex items-center space-x-2 transition duration-300 ease-in-out transform focus:scale-105 focus:outline-none focus:ring-2 focus:ring-red-500"></button>
    <% if (direction === 'left') { %>
        <span class="inline-block transform rotate-180">&larr;</span>
    <% } else if (direction === 'right') { %>
        <span class="inline-block">&rarr;</span>
    <% } else if (direction === 'up') { %>
        <span class="inline-block transform rotate-90">&larr;</span>
    <% } else if (direction === 'down') { %>
        <span class="inline-block transform -rotate-90">&larr;</span>
    <% } %>
    <span><%= content %></span>
</button>